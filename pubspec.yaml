name: jaspr_portfolio
description: Jaspr clone of the existing portfolio site
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  jaspr: ^0.22.1

dev_dependencies:
  build_runner: ^2.10.0
  build_web_compilers: ^4.4.8
  jaspr_builder: ^0.22.1

jaspr:
  mode: static

# Add assets for images (profile, icons, etc.)
flutter:
  assets:
    - assets/images/

